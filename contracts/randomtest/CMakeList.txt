add_contract(randomtest randomtest ${CMAKE_CURRENT_SOURCE_DIR}/src/randomtest.cpp)
target_include_directories(randomtest
   PUBLIC
   ${CMAKE_CURRENT_SOURCE_DIR}/include)

set_target_properties(randomtest
   PROPERTIES
   RUNTIME_OUTPUT_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}")
